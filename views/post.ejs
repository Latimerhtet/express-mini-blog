<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/style.css" />
  </head>
  <body>
    <h2 class="title"><%= targetPost.title %></h2>
    <img src="<%= targetPost.image_url %>" alt="postimage" />
    <p class="paragraph"><%= targetPost.description %></p>
    <div class="btn-div">
      <a href="/">Back</a>
      <form action="/admin/post/<%= targetPost.id %>" method="post">
        <button>Delete Post</button>
      </form>
      <a href="/admin/editPost/<%= targetPost.id %>">Edit Post</a>
    </div>
  </body>
</html>
